import { HomeView } from 'src/sections/home/view';

export const metadata = {
  title: 'Optimus | Call Center',
  description:
    'La solución ideal para llevar la atención al cliente al siguiente nivel.',
};

export default function Page() {
  return <HomeView />;
}
